import React from "react"
import ReactDOM from "react-dom"
import { AppHolder } from "./features/appHolder/appHolder"
import "./index.css"

ReactDOM.render(
  <React.StrictMode>
    <AppHolder />
  </React.StrictMode>,
  document.getElementById("root")
)
